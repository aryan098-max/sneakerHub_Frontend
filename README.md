# Sneaker Hub
- All the packages required
1. Install vite+react, setup vite+react
2. Install tailwindcss, setup tailwindcss
3. Install daisy ui, setup daisy ui
4. Install react router
    - Import {createBrowserRouter,Outlet, RouterProvider} from react-router
5. npm install @heroicons/react - for icons 
    - import from "@heroicons/react/24/solid";
6. npm install react-countdown - for Sale ends in 1:23:45
    - import Countdown (d small) from react-countdown
7.npm install lucide-react
    import {ChevronLeft, ChevronRight} from "lucide-react";


# Building Process
1. Header - Made it responsive
2. Hero Banner - Made it responsive 
3. Hero Banner 2 - Made it responsive 
4. Footer
5. Small Caraousel
    - Caraousel Card
    - line-through (creates discount price)
    - .toLocaleString() (adds comma to number)
6. Entire Set
7. Shop Component
    - Calling Inside banner
    - Defining Shop

